# Ultra-minimal dependencies for Railway deployment
fastapi==0.116.1
uvicorn==0.35.0
streamlit==1.48.1

# Lightweight AI (no torch - use transformers CPU only)
transformers[torch]==4.30.0
tokenizers==0.13.3

# Essential utilities
requests==2.32.5
pydantic==2.11.7
python-dotenv==1.1.1

# Auth (minimal)
passlib==1.7.4
PyJWT==2.10.1

# Text processing (essential only)
textstat==0.7.8